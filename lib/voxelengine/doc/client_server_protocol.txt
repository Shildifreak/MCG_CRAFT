
server -> client
	setup setup_source
		must be sent as first message to newly connected client and specifies where to find texturepack etc.
		setup_source is json object and may contain entries for host, port
	clear generator_data
		lösche alle Blöcke, setze monitored area zurück,
                setze Position des Spielers auf unbekannt, initialisiere neuen terrain generator
	del x y z
		lösche den Block
	delarea xmin ymin zmin xmax ymax zmax
		lösche alle Blöcke in dem Gebiet
	set x y z block
		setze block an stelle x y z
	goto x y z
		setze Kameraposition, starte Neuberechnung von monitored area
	focusdist n
		setze Distanz bis zu der Blöcke anvisiert werden
	setentity id model x y z yaw pitch
		zeige/update Entity
	delentity id
		lösche Entity
	sethud id texture x y z rotation size align
		zeige HUD Element
	delhud
		lösche HUD Element
	focushud
		bringe Client in Mausmodus
	error some msg
		Client hat irgendwas falsch gemacht

	setarea ... deprecated, for now

client -> server
	tick
	
	rot x y
	
	keys bitmap
	
	monitor x_min y_min z_min x_max y_max z_max m_id
	
	update x_min y_min z_min x_max y_max z_max m_id
	
	control entity_id password

	scrolling: n
	
	left click
	
	right click
	
	left clicked hud_id
	
	right clicked hud_id
	
	inv


laden einer Welt:
	client				server
		<---- clear -----
		<---- goto ------
